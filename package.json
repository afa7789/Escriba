{
  "name": "escriba",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based audio transcription using Whisper",
  "scripts": {
    "serve": "serve .",
    "lint": "eslint src/ worker.js",
    "lint:fix": "eslint src/ worker.js --fix",
    "test": "wtr 'tests/**/*.test.js' --node-resolve --playwright --browsers chromium",
    "test:watch": "wtr 'tests/**/*.test.js' --node-resolve --playwright --browsers chromium --watch"
  },
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@web/test-runner": "^0.19.0",
    "@web/test-runner-playwright": "^0.11.0",
    "eslint": "^9.0.0",
    "serve": "^14.2.0"
  }
}
